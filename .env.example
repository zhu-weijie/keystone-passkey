# PostgreSQL Database Configuration
# These variables will be used by the 'db' service to initialize the database
# and by the 'web' service to connect to it.
PGHOST=db
PGPORT=5432
PGUSER=keystone
PGPASSWORD=supersecret
PGDATABASE=keystone_dev

# Application Configuration
PORT=3000
